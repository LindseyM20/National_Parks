package com.casestudy.models;

import javax.persistence.Entity;
import javax.persistence.Id;

@Entity
public class Park {
	@Id
	int id;
	
	// Finish this later if the entity is needed (maybe not needed if only needs to be read?)

}
